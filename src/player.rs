use crate::characters::Character;
use crate::world;

pub struct PlayerInput {

}

pub struct Player {

}

/// Describes that happens in the simulated Game World is simulated in
trait PartyContext: world::WorldContext {
}
